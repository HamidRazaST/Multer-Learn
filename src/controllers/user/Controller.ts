import { Request, Response, NextFunction } from 'express';

class Controller {
    public getImage(req: Request, res: Response, next: NextFunction) {
    }

    public uploadImage(req: Request, res: Response, next: NextFunction) {
    }

    public uploadImages(req: Request, res: Response, next: NextFunction) {
    }
}

export default Controller;